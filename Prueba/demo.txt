# Sistema FALSO de registro de notas
# Solo para ejemplo y práctica con Git

class Estudiante:
    def __init__(self, nombre):
        self.nombre = nombre
        self.notas = []

    def agregar_nota(self, nota):
        if 0 <= nota <= 100:
            self.notas.append(nota)
        else:
            print("Error: la nota debe estar entre 0 y 100")

    def promedio(self):
        if len(self.notas) == 0:
            return 0
        return sum(self.notas) / len(self.notas)

    def mostrar_info(self):
        print(f"Estudiante: {self.nombre}")
        print(f"Notas: {self.notas}")
        print(f"Promedio: {self.promedio():.2f}")


# Ejemplo de uso
est1 = Estudiante("Juan Perez")
est1.agregar_nota(90)
est1.agregar_nota(75)
est1.agregar_nota(88)

est1.mostrar_info()
